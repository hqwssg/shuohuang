<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.carbon.report.mapper.ReportDeptSubjectMapper">
    <select id="selectByDeptId" resultType="com.ruoyi.carbon.report.domain.ReportDeptSubject">
        select id, dept_id as deptId, subject_node_id as subjectNodeId,
               energy_allocation as energyAllocation, enabled
        from report_dept_subject
        where dept_id = #{deptId}
    </select>
</mapper>
